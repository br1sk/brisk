public struct Reproducibility {
    /// Internal apple's identifier
    public let appleIdentifier: Int

    /// The name of the reproducibility; useful to use as display name (e.g. 'Sometimes').
    public let name: String
}
